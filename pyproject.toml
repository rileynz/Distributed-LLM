[project]
name = "distributed-llm-universal"
version = "0.3.0"
description = "Cross-platform llama.cpp cluster manager with admin-free portable workers"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = ["cryptography>=45,<47"]

[tool.pytest.ini_options]
testpaths = ["tests"]
